# ─────────────────────────────────────────────
# STAGING environment
# Used when building for staging: pnpm build
# Set NODE_ENV=production, but point to staging backend.
# ─────────────────────────────────────────────

# Backend API base URL (staging server — update to your real staging URL)
NEXT_PUBLIC_API_URL=http://195.110.58.111:8010/api/v1

# App metadata
NEXT_PUBLIC_APP_NAME=MindNote
NEXT_PUBLIC_APP_ENV=staging
